# Як підключитись до проєкту

Коротка інструкція для команди. Розрахована на те, що ти ніколи не працював з git.
Читай по порядку, нічого не пропускай.

---

## Один раз на початку

### 1. GitHub-акаунт

Якщо немає — реєструйся на github.com. Нікнейм скинь Яну, він додасть тебе
в репозиторій. На пошту прийде запрошення — прийми його, інакше нічого не побачиш.

### 2. GitHub Desktop

Качай з desktop.github.com, встановлюй, логінься своїм акаунтом.
Командний рядок не потрібен, все кнопками.

### 3. Забрати проєкт до себе

У GitHub Desktop: File → Clone repository → вкладка GitHub.com → вибираєш
`game-dojo` → Local path вкажи куди зручно (наприклад `D:\gamejam\game-dojo`) → Clone.

Тепер у тебе локальна копія всього проєкту.

### 4. Node.js

Потрібен, щоб запускати гру локально. Качай з nodejs.org, бери LTS-версію,
встановлюй з усіма галочками за замовчуванням.

### 5. Редактор

Постав VS Code (code.visualstudio.com). У блокноті код правити — біль.

---

## Щодня, коли сідаєш працювати

### Крок 1. Забрати чужі зміни

Відкриваєш GitHub Desktop → **Fetch origin** угорі → якщо кнопка змінилась
на **Pull origin**, тисни її.

Це найважливіше правило в усій інструкції. Не зробив Pull — працюєш на старій
версії, а потім твої зміни конфліктують з чужими, і розгрібати це доведеться Яну.

**Прийшов працювати → спершу Pull. Завжди.**

### Крок 2. Запустити гру локально

Відкриваєш папку проєкту в провіднику. Клацаєш у адресний рядок зверху,
стираєш шлях, пишеш `cmd`, Enter. Відкриється чорне вікно.

Пишеш туди:

    npx serve .

Перший раз спитає `Ok to proceed? (y)` — тиснеш `y`, Enter.

Він покаже адресу `http://localhost:3000` — відкривай її в браузері.
Чорне вікно не закривай, поки працюєш. Зупинити сервер потім — Ctrl+C.

Змінив файл → зберіг → F5 у браузері → бачиш результат.

### Крок 3. Працювати

Про те, який файл твій — нижче.

### Крок 4. Віддати зміни

GitHub Desktop сам показує все, що ти намінял. Зліва внизу поле Summary —
пиши коротко і зрозуміло, що зробив: `нові спрайти героя`, `виправив рівень 2`.
Тиснеш **Commit to main**, потім угорі **Push origin**.

Через півхвилини зміни автоматично зʼявляться на робочому сайті.

---

## Хто який файл чіпає

Головне правило: **двоє не правлять один файл одночасно.** Якщо треба залізти
в чуже — напиши в чат, домовтесь.

| Файл | Що там | Хто |
|---|---|---|
| `src/game.js` | логіка гри, рух, колізії, рівні | код — тільки Ян |
| `src/main.js` | звʼязок гри з інтерфейсом | код — тільки Ян |
| `src/db.js` | лідерборд, Supabase | код — тільки Ян |
| `src/config.js` | ключі й розміри поля | не чіпати |
| `index.html` | розмітка і стилі сторінки | UI/UX |
| `assets/` | спрайти, тайлсети, звук | художники |

Якщо твоя робота — тільки картинки, тобі достатньо класти файли в `assets/`
і робити Commit + Push. У код лізти не треба взагалі.

---

## Спрайти

Формат — **спрайтшит**, тобто одна PNG-картинка з сіткою кадрів. Не GIF.

Герой: файл `assets/hero.png`, кадр 32×32 пікселі, 4 кадри в рядку,
4 рядки в порядку зверху вниз:

    рядок 1 — рух вниз
    рядок 2 — рух вліво
    рядок 3 — рух вправо
    рядок 4 — рух вгору

Тобто загальний розмір картинки 128×128.

Зберігати як PNG з прозорістю. Без згладжування по краях — у грі вимкнена
інтерполяція, розмиті краї виглядатимуть брудно.

Поки файлу немає, гравець малюється блакитним квадратом. Як тільки покладеш
`hero.png` у `assets/` і запушиш — підхопиться саме, код правити не треба.

---

## Якщо щось зламалось

**Біла сторінка.** Натисни F12, вкладка Console. Скопіюй червоний текст і кинь Яну.

**`npx` не розпізнається як команда.** Node.js не встановлений або термінал
відкритий зі старими налаштуваннями. Встанови Node, закрий чорне вікно і відкрий заново.

**GitHub Desktop пише про конфлікт.** Не намагайся розібратись сам — пиши Яну.
Нічого не втрачено, це виправляється, але руками.

**Гра відкривається, а лідерборд пише «Supabase не підключений».**
Швидше за все `src/config.js` побитий. Пиши Яну.

**Зробив дурню і хочеш відкотити.** У GitHub Desktop вкладка History, правою
кнопкою по своєму коміту → Revert. Якщо вже запушив — теж працює, просто
не забудь Push після цього.

---

## Що не робити

Не клади в репозиторій великі файли — вихідники Photoshop, відео, архіви.
Тільки готові експортовані ассети. Репозиторій роздується, і клонування
почне займати вічність.

Не міняй `src/config.js`. Там ключі до бази, і якщо їх зіпсувати,
лідерборд відвалиться у всіх.

Не пуш зламане. Перевір локально через `npx serve .`, що гра запускається,
і тільки потім Push. Робочий сайт оновлюється автоматично — те, що ти
запушив, за півхвилини побачать усі.
